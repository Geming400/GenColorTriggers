{
	"geode": "5.0.1",
	"gd": {
		"win": "2.2081",
		"mac": "2.2081",
		"android": "2.2081",
		"ios": "2.2081"
	},
	"id": "geming400.gen-color-triggers",
	"name": "Gen color triggers",
	"version": "v1.1.8",
	"developer": "Geming400",
	"description": "Generate color triggers for each color channels",
	"links": {
		"source": "https://github.com/Geming400/GenColorTriggers"
	},
	"issues": {
		"info": "Use the 'New issue' button to create an issue. (You need a github account). You can instead dm on discord (@Geming400)",
		"url": "https://github.com/Geming400/GenColorTriggers/issues"
	},
	"tags": [
		"offline",
		"editor",
		"utility"
	],
	"resources": {
		"spritesheets": {
			"genColorTriggers-button": [
				"./resources/genColorTriggers.png"
			]
		}
	},
	"settings": {
		"general-title": {
			"name": "General",
			"type": "title"
		},
		"generate-color-triggers": {
			"type": "keybind",
			"name": "Generate color triggers",
			"description": "Opens the GUI to generate the color triggers. If the show gui setting has been disabled, it will simply generate them instead",
			"default": "U",
			"migrate-from": "geming400.gen-color-triggers/genColorTriggers",
			"category": "editor"
		},
		"show-ui": {
			"name": "Show UI",
			"type": "bool",
			"default": true,
			"description": "Will the UI show ? If no, the generator options will be picked from those settings"
		},
		"include-builtin-color-channels": {
			"name": "Include built-in color channels",
			"type": "bool",
			"default": true,
			"description": "Will color channels such as `BG`, `G`, `MG`, etc... get included in the generated output as well"
		},
		"show-editor-button": {
			"name": "Show editor button",
			"type": "bool",
			"default": false,
			"platforms": ["desktop"],
			"description": "Used to make a button appear in the 'edit' tab of the editor. This is enabled by default on mobile"
		},
		"use-legacy-hsv" : {
			"name": "Use legacy HSV",
			"type": "bool",
			"default": true,
			"description": "This setting will determine if newly generated color triggers will have the 'Legacy HSV' option turned on or off"
		},

		"offsets-title": {
			"name": "Offsets",
			"type": "title"
		},
		"use-gd-grid-space": {
			"name": "Small steps",
			"type": "bool",
			"default": false,
			"description": "If disabled, it will center the given coordinate on the grid. If enabled: 1 grid unit = 30 X/Y. The 'Small Steps' option in the move trigger is very similar"
		},
		"offset-x": {
			"name": "X offset",
			"type": "float",
			"default": 0.0
		},
		"offset-y": {
			"name": "Y offset",
			"type": "float",
			"default": 2.0
		},

		"prettify-trigger-pos-title": {
			"name": "Prettify trigger positions",
			"type": "title"
		},
		"prettify-trigger-pos": {
			"name": "Prettify trigger positions",
			"type": "bool",
			"default": true,
			"description": "Instead of putting each triggers in 1 column, it will separate the number of triggers into multiple columns. The number of column is calculated with the 'For each' setting"
		},
		"for-each-triggers": {
			"name": "For each",
			"type": "int",
			"default": 15,
			"min": 1,
			"description": "How many triggers make 1 column",
			"enable-if": "prettify-trigger-pos",
			"control": {
				"arrows": true,
				"arrow-step": 1,
				"big-arrows": true,
				"big-arrow-step": 5
			}
		}
	}
}
